# 项目功能实现分析

## 技术栈

### 前端
- **框架**：Vue 3 (使用Composition API)
- **样式**：Tailwind CSS
- **图标**：Google Material Symbols
- **文件**：`/Users/jianghongyu/Projects/easier-nju-lib/index.html`

### 后端
- **框架**：FastAPI
- **数据库**：SQLite
- **密码加密**：passlib (使用pbkdf2_sha256算法)
- **邮箱验证**：SMTP
- **文件**：
  - 主后端逻辑：`/Users/jianghongyu/Projects/easier-nju-lib/main.py`
  - 搜索服务：`/Users/jianghongyu/Projects/easier-nju-lib/service.py`
  - 数据模型：`/Users/jianghongyu/Projects/easier-nju-lib/schemas.py`

### 后台管理
- **前端**：独立HTML文件
- **文件**：
  - 登录页面：`/Users/jianghongyu/Projects/easier-nju-lib/templates/admin_login.html`
  - 登录错误页面：`/Users/jianghongyu/Projects/easier-nju-lib/templates/admin_login_error.html`
  - 管理面板：`/Users/jianghongyu/Projects/easier-nju-lib/templates/admin_dashboard.html`

## 功能实现思路

### 1. 邮箱注册功能

#### 实现思路
1. **前端**：
   - 点击"注册"按钮打开注册模态框
   - 用户输入邮箱、密码
   - 点击"获取验证码"按钮，前端向后端发送请求
   - 后端检查邮箱是否已注册，未注册则发送验证码
   - 用户输入验证码后点击"注册"按钮，前端向后端发送注册请求
   - 后端验证验证码是否有效，有效则创建用户

#### 相关代码
- **前端**：`index.html`中的注册模态框和相关Vue逻辑
  - 注册表单：包含邮箱、密码、验证码输入框
  - `sendVerificationCode`函数：发送验证码请求
  - `handleRegister`函数：处理注册请求

- **后端**：`main.py`中的注册相关API
  - `/auth/send-code`端点：发送验证码
  - `/auth/register`端点：处理注册请求
  - 验证码检查和用户创建逻辑

### 2. 用户登录功能

#### 实现思路
1. **前端**：
   - 点击"登录"按钮打开登录模态框
   - 用户输入邮箱、密码
   - 点击"登录"按钮，前端向后端发送登录请求
   - 后端验证用户凭据，返回登录结果

#### 相关代码
- **前端**：`index.html`中的登录模态框和相关Vue逻辑
  - 登录表单：包含邮箱、密码输入框
  - `handleLogin`函数：处理登录请求

- **后端**：`main.py`中的登录API
  - `/auth/login`端点：处理登录请求
  - 用户凭据验证逻辑

### 3. 后台管理页面

#### 实现思路
1. **登录验证**：
   - 访问`/admin`时需要先登录
   - 登录页面验证密码是否正确
   - 密码正确则跳转到管理面板

2. **管理面板**：
   - 显示统计数据：用户数量、验证码数量
   - 显示最近20条搜索记录，包括时间、IP、提示词、LLM响应、token消耗
   - 显示系统信息：运行状态、当前时间

#### 相关代码
- **前端**：`templates`目录下的HTML文件
  - `admin_login.html`：登录页面
  - `admin_login_error.html`：密码错误页面
  - `admin_dashboard.html`：管理面板

- **后端**：`main.py`中的后台管理路由
  - `/admin/login`端点：处理登录请求
  - `/admin`端点：显示管理面板
  - 统计数据获取和日志解析逻辑

### 4. 邮箱重复检测

#### 实现思路
1. **后端**：
   - 在发送验证码前检查邮箱是否已注册
   - 如果邮箱已注册，返回错误提示，不发送验证码
   - 如果邮箱未注册，正常发送验证码

#### 相关代码
- **后端**：`main.py`中的验证码发送逻辑
  - `/auth/send-code`端点：发送验证码前检查邮箱是否已注册
  - `/auth/check-email`端点：检查邮箱是否已注册（可选）

### 5. 验证码发送

#### 实现思路
1. **后端**：
   - 生成6位验证码
   - 检查是否已有未使用的验证码
   - 插入或更新验证码记录
   - 通过SMTP发送验证码到用户邮箱

#### 相关代码
- **后端**：`main.py`中的验证码发送逻辑
  - `send_code`函数：生成验证码并发送
  - `send_verification_email`函数：发送邮件

### 6. 日志记录

#### 实现思路
1. **后端**：
   - 记录用户搜索操作，包括时间、IP、提示词等
   - 记录LLM调用信息，包括输入、输出、token消耗
   - 解析日志文件，提取搜索记录
   - 在后台管理页面展示搜索记录

#### 相关代码
- **后端**：`main.py`中的日志记录和解析逻辑
  - 搜索记录日志：在搜索API中记录
  - LLM调用日志：在LLM调用中记录
  - `admin_dashboard`函数：解析日志并展示

## 数据流程

### 注册流程
1. 用户输入邮箱、密码
2. 前端发送验证码请求
3. 后端检查邮箱是否已注册
4. 后端生成验证码并发送到邮箱
5. 用户输入验证码
6. 前端发送注册请求
7. 后端验证验证码
8. 后端创建用户并保存到数据库

### 登录流程
1. 用户输入邮箱、密码
2. 前端发送登录请求
3. 后端验证用户凭据
4. 后端返回登录结果

### 搜索流程
1. 用户输入搜索提示词
2. 前端发送搜索请求
3. 后端调用LLM分析提示词
4. 后端生成搜索请求体
5. 后端调用官方API获取搜索结果
6. 后端记录搜索操作和LLM调用信息
7. 后端返回搜索结果给前端

### 后台管理流程
1. 用户访问`/admin`
2. 后端重定向到登录页面
3. 用户输入密码
4. 后端验证密码
5. 后端跳转到管理面板
6. 后端获取统计数据和搜索记录
7. 后端渲染管理面板页面

## 安全措施

1. **密码加密**：使用passlib的pbkdf2_sha256算法对密码进行哈希处理
2. **验证码验证**：注册时需要验证邮箱验证码
3. **后台管理密码**：后台管理页面需要密码登录
4. **日志记录**：记录用户操作，便于审计和排查问题

## 总结

该项目实现了一个图书馆智能检索系统，主要功能包括：
- 自然语言搜索图书
- 邮箱注册和登录
- 后台管理面板
- 详细的日志记录

技术栈选择合理，前端使用Vue 3和Tailwind CSS实现响应式界面，后端使用FastAPI提供高效的API服务，数据库使用SQLite存储数据，邮箱验证使用SMTP发送验证码。

代码结构清晰，功能实现完整，安全性考虑充分，是一个设计良好的Web应用项目。